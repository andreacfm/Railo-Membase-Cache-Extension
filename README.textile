h4. Not supported Attributes

* timeidle

h4. Not supported Functions

* cacheCount()
* cacheGetAllIds()
* cacheGetAll()
* cacheGetMetadata()

h3. Getting Started
 	
* Run moxi client as standalone service. Connect the Railo instance to localhost:11211 ( moxi default host/port).

pre. 
moxi http://{membase server1}:8080/pools/default/bucketsStreaming/default http://{membase server2}:8080/pools/default/bucketsStreaming/default

p. The local moxi client will manage the cluster of the passed membase servers ( the 2 servers must be explicitly clusterized in the memebase admin for 
managing the failover )

h3. Important
 
* Double Serialization 
